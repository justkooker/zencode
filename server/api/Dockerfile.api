FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY db.json ./

EXPOSE 3000

CMD ["npx", "json-server", "--watch", "db.json", "--port", "3000", "--host", "0.0.0.0"]
