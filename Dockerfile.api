FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY db.json ./

EXPOSE 3001

CMD ["npx", "json-server", "--watch", "db.json", "--port", "3001", "--host", "0.0.0.0"]
